# 官网流量分析智能体 — 执行清单

> 部署目标：现官网（www.ymin.com）子目录 `/agent/`  
> 智能体名称：官网小铭同学  
> 数据来源：百度统计API + 现有MySQL产品库 + DeepSeek大模型  

---

## 总览

```
百度统计API ──┐
MySQL产品库 ──┼──→ Node.js 数据服务 (端口5000) ──→ /agent/ 前端页面
DeepSeek API ─┘                                     │
                                                    └── 钉钉/飞书推送
```

---

## 一、数据库准备

### 1.1 现有数据库已确认

| 项目 | 值 |
|------|-----|
| 数据库名 | `yongming` |
| 产品表 | `tp_good`（约上万条） |
| URL→产品映射 | URL 参数 `part_number` = `tp_good.liaohao`（料号） |
| 产品系列 | `tp_good.xl` |
| 应用领域 | `tp_good.lingyu`（已有字段） |
| 产品型号 | `tp_good.pinxing` |
| 栏目分类 | `tp_column`（`pid` 支持层级分类） |
| 系列管理 | `tp_xilie`（关联 `column_id`） |

> ⚠️ 需要你在服务器 PHPMyAdmin 里跑一条 SQL 确认产品线分布，见下方。

### 1.2 新建流量分析专用表（在现有库里加 3 张表）

```sql
-- 表1：每日流量明细（从百度统计拉取的数据）
CREATE TABLE IF NOT EXISTS `traffic_daily` (
  `id` int AUTO_INCREMENT PRIMARY KEY,
  `date` date NOT NULL,
  `page_url` varchar(500) NOT NULL,
  `page_type` varchar(50) DEFAULT NULL COMMENT '产品页/文章/FAQ/设计工具',
  `pv` int DEFAULT 0,
  `uv` int DEFAULT 0,
  `avg_stay` int DEFAULT 0 COMMENT '平均停留秒数',
  `bounce_rate` decimal(5,2) DEFAULT 0 COMMENT '跳出率',
  `created_at` timestamp DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY `uk_date_url` (`date`, `page_url`(200)),
  KEY `idx_date` (`date`),
  KEY `idx_page_type` (`page_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 表2：搜索词数据
CREATE TABLE IF NOT EXISTS `traffic_search_terms` (
  `id` int AUTO_INCREMENT PRIMARY KEY,
  `date` date NOT NULL,
  `keyword` varchar(300) NOT NULL,
  `pv` int DEFAULT 0,
  `landing_url` varchar(500) DEFAULT NULL,
  `source` varchar(50) DEFAULT 'baidu',
  `created_at` timestamp DEFAULT CURRENT_TIMESTAMP,
  KEY `idx_date` (`date`),
  KEY `idx_keyword` (`keyword`(100))
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 表3：预警记录
CREATE TABLE IF NOT EXISTS `traffic_alerts` (
  `id` int AUTO_INCREMENT PRIMARY KEY,
  `alert_time` datetime NOT NULL,
  `alert_type` enum('surge','decay','zero_traffic') NOT NULL COMMENT '飙升/衰减/僵尸',
  `target_name` varchar(300) NOT NULL COMMENT '产品名或文章名',
  `target_url` varchar(500) DEFAULT NULL,
  `change_rate` varchar(50) DEFAULT NULL COMMENT '变化率',
  `reason_hint` text COMMENT '推测原因',
  `status` enum('new','read','resolved') DEFAULT 'new',
  `created_at` timestamp DEFAULT CURRENT_TIMESTAMP,
  KEY `idx_alert_time` (`alert_time`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 1.3 确认产品线映射（需你在服务器上执行）

在 PHPMyAdmin → `yongming` 库 → SQL 窗口，依次跑以下三条查询，把结果复制给我：

**查询 1：看产品分类结构**
```sql
SELECT c.id, c.pid, c.title, COUNT(g.id) as product_count
FROM tp_column c
LEFT JOIN tp_good g ON g.column_id = c.id
WHERE c.pid > 0
GROUP BY c.id, c.pid, c.title
ORDER BY product_count DESC;
```

**查询 2：看系列分布**
```sql
SELECT xl, COUNT(*) as cnt
FROM tp_good
WHERE delete_time IS NULL
GROUP BY xl
ORDER BY cnt DESC
LIMIT 30;
```

**查询 3：看应用领域填充情况**
```sql
SELECT lingyu, COUNT(*) as cnt
FROM tp_good
WHERE delete_time IS NULL AND lingyu IS NOT NULL AND lingyu != ''
GROUP BY lingyu
ORDER BY cnt DESC;
```

---

## 二、后端数据服务搭建

### 2.1 目录结构

```
/www/wwwroot/www.ymin.com/
├── agent/                  ← 前端页面放这里
│   ├── index.html
│   ├── 小铭同学.png
│   └── ...
├── agent-server/           ← 后端服务（新建）
│   ├── package.json
│   ├── server.js           ← 主入口
│   ├── baiduApi.js         ← 百度统计API对接
│   ├── deepseekApi.js      ← DeepSeek大模型调用
│   ├── db.js               ← MySQL连接
│   ├── alerts.js           ← 预警规则引擎
│   ├── reports.js          ← 报告生成
│   └── .env                ← 密钥配置（不要提交git）
```

### 2.2 安装 Node.js 依赖

SSH 连上服务器后：

```bash
cd /www/wwwroot/www.ymin.com
mkdir agent-server
cd agent-server
npm init -y
npm install express mysql2 axios cron node-cron cors dotenv
```

### 2.3 后端核心代码架构

**server.js** — 主入口，提供以下 API：

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/dashboard` | GET | 今日 KPI 概览（PV/UV/自然搜索占比/预警数） |
| `/api/trend` | GET | PV/UV 趋势数据（?days=7） |
| `/api/rankings` | GET | 排行榜（?type=products/articles/tools&period=7d） |
| `/api/applications` | GET | 应用领域洞察 |
| `/api/alerts` | GET | 预警列表 |
| `/api/content` | GET | 内容价值评估 |
| `/api/chat` | POST | 小铭同学对话（调 DeepSeek） |
| `/api/reports` | POST | 生成报告 |

**baiduApi.js** — 百度统计数据拉取UTF-8u
- 调用百度统计 API 获取站点列表
- 拉取各页面 PV/UV/搜索词
- 写入 `traffic_daily` 和 `traffic_search_terms` 表

**deepseekApi.js** — 大模型调用
- 搜索词意图分类（找型号/找替代/找方案/查参数）
- NL对话查询理解（用户自然语言→SQL/API查询）
- 预警原因推测
- 周报摘要生成

### 2.4 配置 .env 文件

```env
# 百度统计
BAIDU_API_KEY=Tm4cDmzq46nbMt0IoDyseyCxJyfqeAAE
BAIDU_SECRET_KEY=UpMpXAEOGiZDNYkERRVoiof1eXd7Kv1d
BAIDU_SITE_ID=你的站点ID

# DeepSeek
DEEPSEEK_API_KEY=sk-3082acabc72a46dd80afaaa46bf17d8a
DEEPSEEK_BASE_URL=https://api.deepseek.com

# MySQL
MYSQL_HOST=127.0.0.1
MYSQL_USER=root
MYSQL_PASSWORD=你的数据库密码
MYSQL_DATABASE=yongming

# 服务端口
PORT=5000
```

---

## 三、前端页面改造

### 3.1 把 Demo 页面改为数据驱动

现有 `traffic-agent-demo.html` 用的是写死的模拟数据，需要改成从 `/api/*` 拉取真实数据。

**主要改动点：**
- KPI卡片、趋势图 → 调 `/api/dashboard` + `/api/trend`
- 排行榜 → 调 `/api/rankings`
- 预警 → 调 `/api/alerts`
- 小铭同学对话 → 调 `/api/chat`

### 3.2 部署到 /agent/

```bash
# 在服务器上创建目录
mkdir -p /www/wwwroot/www.ymin.com/public/agent

# 把文件传上去（在本地执行，或用宝塔上传）
# traffic-agent-demo.html → agent/index.html
# 小铭同学.png → agent/小铭同学.png
```

---

## 四、Nginx 配置（关键步骤）

### 4.1 让 /agent/ 不被 ThinkPHP 拦截

编辑 `/www/server/panel/vhost/nginx/www.ymin.com.conf`，在 `location /` 之前加：

```nginx
# ===== 流量分析智能体 =====
# /agent/ 子目录不走 ThinkPHP 框架
location ^~ /agent/ {
    root /www/wwwroot/www.ymin.com/public;
    index index.html;
    try_files $uri $uri/ /agent/index.html;
}

# /agent-server API 反向代理
location ^~ /api/ {
    proxy_pass http://127.0.0.1:5000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_read_timeout 60s;
}
# ===== 流量分析智能体 结束 =====
```

然后重载 Nginx：
```bash
nginx -t && nginx -s reload
```

### 4.2 验证

浏览器访问 `https://www.ymin.com/agent/` 应该能看到智能体页面（不是 ThinkPHP 404）。

---

## 五、定时任务

宝塔面板 → 计划任务，添加两个：

| 任务 | 脚本 | 频率 |
|------|------|------|
| **拉取百度统计数据** | `cd /www/wwwroot/www.ymin.com/agent-server && node fetchData.js` | 每天凌晨 2:00 |
| **运行预警检测** | `cd /www/wwwroot/www.ymin.com/agent-server && node checkAlerts.js` | 每天凌晨 3:00 |
| **启动服务（保活）** | `cd /www/wwwroot/www.ymin.com/agent-server && node server.js` | 开机启动 |

---

## 六、启动后端服务

### 6.1 用 PM2 管理进程

```bash
npm install -g pm2
cd /www/wwwroot/www.ymin.com/agent-server
pm2 start server.js --name agent-server
pm2 save
pm2 startup  # 设置开机自启
```

### 6.2 检查服务

```bash
pm2 status
curl http://127.0.0.1:5000/api/dashboard
```

---

## 七、执行顺序

按优先级从上到下做：

| 步骤 | 内容 | 预估耗时 | 依赖 |
|------|------|---------|------|
| 1 | PHPMyAdmin 查产品表结构，建 3 张新表 | 30分钟 | — |
| 2 | 建立产品线映射关系 | 1小时 | 步骤1 |
| 3 | 搭建 agent-server，配 .env | 1小时 | — |
| 4 | 写 baiduApi.js（拉取百度统计数据） | 2小时 | 步骤2、3 |
| 5 | 写 server.js + API 接口 | 3小时 | 步骤3、4 |
| 6 | 前端页面改造（接入真实API） | 2小时 | 步骤5 |
| 7 | Nginx 配置 + 部署 /agent/ | 30分钟 | 步骤6 |
| 8 | 配定时任务 + PM2 保活 | 30分钟 | 步骤7 |
| 9 | 调 deepseekApi.js（小铭同学对话） | 2小时 | 步骤5 |
| 10 | 配钉钉/飞书推送 | 1小时 | 步骤8 |
| 11 | 验收：数据准确性校验 | 1小时 | 全部 |

**总计预估：约 14 小时**（1-2 个工作日），建议分 3 天完成。

---

## 八、风险点

| 风险 | 应对 |
|------|------|
| **ThinkPHP 拦截 /agent/** | 已用 `location ^~ /agent/` 精确匹配跳过，优先级最高 |
| **百度统计 API 站点ID 未知** | 需在百度统计后台→管理→站点列表获取 `site_id` |
| **产品表结构未知** | 步骤1 先查表结构，确认字段名后再写 SQL |
| **Python 3.6 太老** | 不用 Python，全部用 Node.js 16（已验证可用） |
