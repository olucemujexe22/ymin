# 现官网服务器（ymin）信息汇总

> 最后更新：2026-06-05

---

## 一、基础信息

| 项目 | 内容 |
|------|------|
| **主机名** | `ecs-e41c` |
| **内网 IP** | `192.168.0.163/24` |
| **操作系统** | **CentOS Linux release 7.9.2009 (Core)** |
| **内核版本** | `3.10.0-1160.105.1.el7.x86_64` |
| **CPU** | 2 核 |
| **内存** | 3.7G（已用 1.5G，可用 1.7G） |
| **磁盘** | 197G（已用 71G，剩余 118G，使用率 38%） |
| **运行时间** | 已运行 20 天 |
| **SSH 端口** | 22（已开启密码登录 + Root 登录） |

---

## 二、Web 服务

| 服务 | 版本 | 说明 |
|------|------|------|
| **Nginx** | 1.22.1 | 宝塔面板管理 |
| **PHP** | 7.2.33 | 现官网 ThinkPHP 框架使用 |
| **Node.js** | v16.20.2 | 手动安装 |
| **Python** | 3.6.8 | 系统自带 |
| **宝塔面板** | ✅ 已安装 | 管理 Web 服务 |

---

## 三、官网网站（www.ymin.com）

| 项目 | 内容 |
|------|------|
| **域名** | `www.ymin.com` + `ymin.com` |
| **网站根目录** | `/www/wwwroot/www.ymin.com/public` |
| **框架** | **ThinkPHP**（PHP 框架） |
| **SSL 证书** | ✅ 已配置（强制 HTTPS 跳转） |
| **Nginx 配置** | `/www/server/panel/vhost/nginx/www.ymin.com.conf` |
| **SSL 证书路径** | `/www/server/panel/vhost/cert/www.ymin.com/fullchain.pem` |
| **SSL 密钥路径** | `/www/server/panel/vhost/cert/www.ymin.com/privkey.pem` |

### Nginx 配置要点
- 监听端口：80（HTTP）+ 443（HTTPS）
- 强制 HTTP → HTTPS 跳转
- 连接限制：单服务器 60，单 IP 10
- 速率限制：512k/s
- SSL 协议：TLSv1.1 ~ TLSv1.3
- 静态资源缓存：图片 30 天，JS/CSS 12 小时

---

## 四、Docker 容器

| 容器名 | 镜像 | 端口映射 | 状态 | 说明 |
|--------|------|---------|------|------|
| **ymin-web** | `test-web1-ymin-web` | `3000:3000` | ✅ 运行中（3小时） | **新官网 Node.js 服务** |
| **cs** | `codercom/code-server:latest` | `8080:8080` | ✅ 运行中（2周） | VS Code Server |

### ymin-web 容器详情
- **运行命令**：`node server.js`
- **工作目录**：`/app`
- **Node.js 版本**：22.22.3
- **环境变量**：`NODE_ENV=production`，`PORT=3000`

---

## 五、防火墙开放端口

```
20/tcp, 22/tcp, 80/tcp, 443/tcp, 888/tcp, 3306/tcp, 8250/tcp,
22680/tcp, 22681/tcp, 39000-40000/tcp
```

---

## 六、网站目录结构

| 目录 | 说明 |
|------|------|
| `/www/wwwroot/www.ymin.com` | **现官网**（ThinkPHP） |
| `/www/wwwroot/test-web1` | 测试站（新官网 Docker 源码） |
| `/www/wwwroot/test-web` | 测试站 |
| `/www/wwwroot/geo.ymin.com` | 地理站点 |
| `/www/wwwroot/ymin.live` | live 站点 |
| `/www/wwwroot/1.94.1.192` | IP 站点 |

### 现官网目录结构（www.ymin.com）
```
/www/wwwroot/www.ymin.com/
├── application/          # 应用目录
├── config/               # 配置目录
├── extend/               # 扩展目录
├── public/               # 入口目录（网站根目录）
├── route/                # 路由目录
├── runtime/              # 运行时缓存
├── thinkphp/             # ThinkPHP 框架
├── vendor/               # Composer 依赖
├── think                 # CLI 入口
├── composer.json         # Composer 配置
└── robots.txt            # 爬虫规则
```

---

## 七、VS Code Remote-SSH 状态

| 项目 | 状态 |
|------|------|
| **VS Code Server 版本** | 1.103.2（`6f17636121051a53c88d3e605c491d22af2ba755`） |
| **Node 兼容方案** | ✅ 已配置 Node 16 包装器（兼容 CentOS 7 glibc） |
| **本地 SSH 客户端** | ✅ 已配置 Git OpenSSH 10.3 |
| **远程平台设置** | ✅ 已设为 `linux` |
| **连接状态** | ⚠️ 已解决 SSH 连接问题，服务器端 glibc 兼容性待完全解决 |

### 已完成的配置
1. ✅ 安装 Remote-SSH 扩展
2. ✅ SSH 密钥连接配置
3. ✅ VS Code Server 1.103.2 部署
4. ✅ Node.js 16 替换（兼容 CentOS 7）
5. ✅ ESM bootloader 配置
6. ✅ 智能包装器脚本
7. ✅ SSH config 格式修复
8. ✅ 本地 OpenSSH 10.3 配置
9. ✅ remotePlatform 设为 linux

---

## 八、SSH 连接方式

```bash
# 通过 SSH 连接服务器
ssh ymin

# 或使用完整命令
ssh root@192.168.0.163
```

### VS Code Remote-SSH 配置
```json
{
    "remote.SSH.path": "C:\\Program Files\\Git\\usr\\bin\\ssh.exe",
    "remote.SSH.remotePlatform": {
        "ymin": "linux"
    }
}