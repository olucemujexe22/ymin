{
  "name": "ymin-cad-library-build",
  "private": true,
  "scripts": {
    "build:cad-geometry": "node build_cad_geometry.js",
    "build:cad-index": "python build_cad_index.py",
    "build:cad": "npm run build:cad-geometry && npm run build:cad-index",
    "build:products": "python build_product_catalog.py",
    "build:data": "npm run build:cad-index && npm run build:products",
    "download:products": "node download_tp_good.js"
  },
  "devDependencies": {
    "occt-import-js": "0.0.23"
  }
}
