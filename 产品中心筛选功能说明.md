# 产品中心 — 电容器类别分组筛选说明

## 分组规则

电容器类别共 8 项（已删除多层陶瓷片式电容器），按产品特性分为 4 组：

| 组名 | 包含类别 | 规则 |
|------|---------|------|
| 铝电解组 | 液态铝电解电容器、高分子固态铝电解电容器、高分子混合动力铝电解电容器 | 组内可多选 |
| 超级电容组 | 双电层超级电容、混合型超级电容（锂离子电容） | 组内可多选 |
| 叠层与钽组 | 叠层高分子固态铝电解电容器、导电高分子钽电解电容器 | 组内可多选 |
| 独立组 | 薄膜电容器 | 可单独勾选 |

## 交互逻辑

- **未选中任何项**：所有复选框均可点击，为默认"全部产品"状态
- **选中某一组中的项**：该组内其他项仍可勾选；其他组的复选框自动置灰禁用
- **取消全部选中**：所有组自动解锁，恢复默认状态
- **「应用筛选重置」按钮**：一键清空所有筛选，恢复全部产品

## 面包屑与标题联动

| 选中数量 | 面包屑 | 标题 | 筛选标签 |
|---------|--------|------|---------|
| 0 项（全部） | 产品中心 / 全部产品 | 全部电容器产品 | 无 |
| 1 项 | 产品中心 / [类别名] | [类别名] | 无（与标题重复，不显示） |
| 2+ 项 | 产品中心 / 筛选结果 | 电容器筛选结果 | 显示已选标签（每个可 × 移除） |

## 技术实现

筛选逻辑由 `product-center.html` 内联 JavaScript 实现：

- `onCatChange(el)` — 复选框变更入口，控制分组互斥
- `enableAll()` — 当所有复选框均未选中时，解锁全部选项
- `getSelectedCategories()` — 获取当前已选类别名称数组
- `updateUI()` — 根据已选状态动态更新面包屑、标题、标签 chips、结果计数
- `removeChip(name)` — 点击标签 × 移除单个筛选条件

无需额外依赖，纯原生 JS。
